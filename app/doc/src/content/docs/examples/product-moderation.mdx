---
title: Product Moderation (Strict Remove)
---

import ComponentPreview from "@/components/component-preview.astro";

### Usage

Demonstrates `removeMode="strict"` where items are deleted server-side first via `onRemove` before being removed from the UI.

<ComponentPreview exampleComponent={"product-moderation"} />
