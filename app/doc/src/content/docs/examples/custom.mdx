---
title: Custom Example
---

import ComponentPreview  from "@/components/component-preview.astro";

### Usage

This example demonstrates a custom layout where the preview is rendered before the trigger.
It uses the same building blocks—`UplofileRoot`, `UplofilePreview`, and `UplofileTrigger`—
to show how you can reorder components to fit your UI.

<ComponentPreview exampleComponent={"custom-example"} />