---
title: Basic
---

import ComponentPreview  from "@/components/component-preview.astro";

### Usage

A minimal setup that renders a custom trigger button and an inline image preview
using `UplofileRoot`, `UplofileTrigger`, and `UplofilePreview`.
No form integration or hidden inputs—just select an image and see it previewed.

<ComponentPreview exampleComponent={"basic-example"} />
